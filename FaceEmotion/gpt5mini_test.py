# =============================================================================
# FaceEmotion — GPT-5-mini (via OpenAI-compatible API)
# =============================================================================
from _api_shared import run_api

if __name__ == "__main__":
    run_api("gpt-5-mini", "FaceEmotion")